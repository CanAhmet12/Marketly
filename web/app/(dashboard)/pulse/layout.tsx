import type { ReactNode } from "react";

import "@/styles/route-groups/discover.css";
import "@/styles/route-groups/media.css";

export default function PulseLayout({ children }: { children: ReactNode }) {
  return children;
}
