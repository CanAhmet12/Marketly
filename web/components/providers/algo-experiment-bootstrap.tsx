"use client";

import { useAlgoExperimentBootstrap } from "@/hooks/use-algo-experiment-bootstrap";

/** Sprint 12 — oturum başına deney impression logları */
export function AlgoExperimentBootstrap() {
  useAlgoExperimentBootstrap();
  return null;
}
